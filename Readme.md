# 📊 Пайплайн данных из API УК «Арсагера» в PostgreSQL

Автоматизированный ETL-пайплайн для загрузки, обработки и анализа данных о портфелях инвестиционных фондов управляющей компании **«Арсагера»** с последующим сохранением в **PostgreSQL** и визуализацией в **Microsoft Power BI**.

---

## 🏦 Поддерживаемые фонды

Пайплайн обрабатывает данные по следующим фондам:
- **Фонд акций**
- **Фонд смешанных инвестиций**
- **Фонд акциИ 6.4**
- **Фонд облигаций КР 1.55**

---

## 🧩 Общая схема работы

1. **Загрузка** данных через официальное API УК «Арсагера» (`requests`)
2. **Преобразование** с помощью `pandas`
3. **Загрузка** во временные таблицы в PostgreSQL через `sqlalchemy`
4. **Агрегация** и обогащение данных с использованием SQL-запросов (`psycopg2`)
5. **Очистка**: удаление временных таблиц после завершения пайплайна (`psycopg2`)
6. **Визуализация**: отчёт в Power BI на основе итоговой таблицы

Все этапы запускаются из главного скрипта — **`main.py`**.

---

## 📁 Структура проекта

```
Arsagera_funds/
├── main.py                     # Главный скрипт — точка входа
├── py_scripts/                 # Пользовательские модули
├── arsagera_api.pdf            # Документация API УК «Арсагера»
├── Arsagera_funds.pbix         # Power BI-отчёт по эффективности фондов
├── requirements.txt            # Зависимости Python
├── .gitignore                  # Исключает cred.json и env/
└── README.md
```

> 🔒 **Важно**:  
> - Файл `cred.json` с учётными данными **не включён** в репозиторий.  
> - Папка виртуального окружения `env/` также исключена.  
> Оба элемента перечислены в `.gitignore`.

---

## ⚙️ Требования и установка

### Требования
- Python 3.9+
- PostgreSQL 12+

### Установка

```bash
# 1. Клонирование репозитория
git clone https://github.com/AlekseyZykov/Arsagera_funds.git
cd Arsagera_funds

# 2. Создание и активация виртуального окружения
python -m venv env
source env/bin/activate      # Linux/macOS
# или
env\Scripts\activate         # Windows

# 3. Установка зависимостей
pip install -r requirements.txt
```

### Настройка подключения (`cred.json`)

Создайте файл `cred.json` в корне проекта (локально!):

```json

 {
    "host": "localhost",
    "port": 5432,
    "dbname": "postgres",
    "user": "postgres",
    "password": "your_password"
  }
```

---

## ▶️ Запуск

```bash
python main.py
```

После завершения в PostgreSQL будут обновлены итоговые таблицы, а временные объекты — удалены.

---

## 📈 Power BI отчёт

Файл **`Arsagera_funds.pbix`** содержит интерактивный дашборд с:
- Доходностью фондов
- Динамикой СЧА (стоимости чистых активов)
- Сравнением эффективности управления

Для актуализации данных настройте подключение Power BI к вашей PostgreSQL-базе.

---

## 📚 Документация

Подробное описание API содержится в файле **`arsagera_api.pdf`**.

---

## ⚠️ Важно

- Все конфиденциальные данные хранятся **локально** и не попадают в публичный репозиторий.
- Рекомендуется использовать отдельного пользователя БД с минимальными необходимыми правами.

---

## 📬 Автор

📧 **AlekseyZykov86@yandex.ru**  
💼 [https://github.com/AlekseyZykov](https://github.com/AlekseyZykov)